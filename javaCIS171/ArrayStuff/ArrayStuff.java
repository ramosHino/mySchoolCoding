public class ArrayStuff
{
public static void main(String[]args)
   {
       int[] temp;
       
       temp=new int[5];
       
       float[]rates=new float[10];
       final int SIZE=5
       int[]temps=new int[SIZE]
       
       double[]rent={550.00,780.00,950.00};//intializes array
       int temp[],rates,counts;
       int[] temp,rates,counts;//creates three arrays
       mySize=temp.length;
       
       // dont do this creates garbage memory
       temp=new int[5]
       temp=new int[10]
       
       //garbage data example
       float[] amts,rates;
       amts= new float[5]
       rates= new float[5]
       amts=rate
       
       
       for(int x=0; x<temp.length;++x)
          {
          
          }
          
       for(int x=0;x<rates.length;++x)
       {
         rates[x]=0
       }   
       for(float currentRate : rates)//it displays all the elements in array one by one, it increments itself
         {
         System.out.println(" "+currentRate);
         }
       
       //both of these do the same thing just different way of doing it
       for(int x=0;x<orders.length;++x) 
       {
       orders[x].display();
       }
       for (Pizza onePizza:orders)
       {
         onePizza.display();
       }
       
   }

}
